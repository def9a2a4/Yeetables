# Yeetables Plugin Configuration
# Global settings only - see yeetables.yml and items.yml for projectiles and items

# Enable debug logging
debug: false

# Hide the actual snowball projectile when using block/item display renderers
# Set to false for debugging to see the snowball alongside display entities
hide-display-projectiles: true

# Worlds where Yeetables should be fully disabled.
# In these worlds the plugin will not intercept interact/shoot/hit/unleash events at all.
disabled-worlds:
  - lbwlobby

# Entities exempt from swap ability teleportation
# Each entry can have:
#   type: ENTITY_TYPE (e.g. ARMOR_STAND, VILLAGER) or "*" for any entity
#   if-has-any-tag: true (exempt if entity has ANY scoreboard tag)
#   if-has-tags: [list, of, tags] (exempt if entity has specific tags)
#   tag-match-mode: ANY or ALL (default: ANY)
swap-exempt-entities:
  - type: ARMOR_STAND
    if-has-any-tag: true
  - type: SHULKER
    if-has-any-tag: true
